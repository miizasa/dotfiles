;; -*- mode: emacs-lisp -*-
;; History entries used for anything adaptive display.
(setq anything-c-adaptive-history (quote ((command ("anything-google-suggest" ("" . 1)) (anything-google-suggest ("goo" . 1))))))
